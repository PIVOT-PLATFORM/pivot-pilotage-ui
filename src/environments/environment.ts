// Squelette bootstrap-only : aucun appel backend aujourd'hui (pas de feature réelle,
// voir CLAUDE.md). Forme alignée sur pivot-ui pour cohérence future — `apiUrl` sera
// consommé une fois les premières features Pilotage développées.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
};
